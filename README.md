# MongoDBChallenge

A simple command line app the flattens a JSON object. The app expects input via ``stdin`` and takes no other arguments.

To run the app via the command line use ``cat test.json | python app.py`` to pass the content of ``test.json``to the app.

